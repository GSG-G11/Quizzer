import {
  successSignup, takenEmail, invalidPassword, invalidUsername, invalidAvatar,
} from './signup-models';
import {
  successLogin, incorrectEmail, invalidUserPassword, incorrectPassword,
} from './login-models';

export {
  successSignup,
  takenEmail,
  invalidPassword,
  invalidUsername,
  invalidAvatar,
  successLogin,
  incorrectEmail,
  invalidUserPassword,
  incorrectPassword,
};
